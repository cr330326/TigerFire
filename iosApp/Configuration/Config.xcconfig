TEAM_ID=

PRODUCT_NAME=TigerFire
PRODUCT_BUNDLE_IDENTIFIER=com.cryallen.tigerfire.TigerFire$(TEAM_ID)

CURRENT_PROJECT_VERSION=1
MARKETING_VERSION=1.0

// Kotlin Framework - use more flexible path
FRAMEWORK_SEARCH_PATHS = $(inherited) \
    $(SRCROOT)/../composeApp/build/xcode-frameworks/** \
    $(SRCROOT)/../composeApp/build/bin/**/Framework

// Other linker flags to ensure framework is found
OTHER_LDFLAGS = $(inherited) -framework ComposeApp