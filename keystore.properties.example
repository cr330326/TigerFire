# Keystore 配置文件示例
# 复制此文件为 keystore.properties 并填入实际的值
# 注意：keystore.properties 不要提交到版本控制

# Keystore 文件路径 (相对于项目根目录或绝对路径)
storeFile=release.keystore

# Keystore 密码
storePassword=your_keystore_password

# Key 别名
keyAlias=release

# Key 密码
keyPassword=your_key_password
